
= Manual de Integración (migrado)

Este contenido fue migrado al nuevo directorio `integration/modules/ROOT/pages/index.adoc`.

Por compatibilidad, el antiguo árbol `implementation/` se mantiene como referencia, pero el contenido principal ahora vive en `integration/`.

== Nota

Favor referirse al nuevo directorio `integration/` para el contenido actualizado.
